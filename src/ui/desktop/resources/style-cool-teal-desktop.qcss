QWidget {
    font-family: "Noto Sans";
    background-color: #1e1f28;
    color: #aeb5c0;
    font-size: 13px;
}

QPushButton:hover {
    border: 1px solid #3ddcff;
}

QPushButton:pressed {
    background-color: #3ddcff;
    color: #0b0d12;
}

QTreeView::item:selected,
QListView::item:selected {
    background-color: #2a2c3a;
    color: #e6eaf2;
}

QTreeView::item:hover,
QListView::item:hover {
    background-color: #242632;
}

#container {
    border-radius: 8px;
}

/* Top bar and panels */

#NfTopBar, #NfPanel, #NfMainMenu {
    background-color: #242632;
    border: 1px solid #323546;
    border-radius: 2px;
}

#NfMainMenu {
    border: none;
}

/* Top bar */

#NfTopBar {
    border-top: 3px solid #323546;
    border-left: 1px solid #323546;
    border-right: 1px solid #323546;
    border-top-left-radius: 8px;
    border-top-right-radius: 8px;
    margin-bottom: 4px;
    padding: 2px;
    padding-left: 5px;
}

#NfTopBar QPushButton#btnMinimize,
#NfTopBar QPushButton#btnMaximize,
#NfTopBar QPushButton#btnClose {
    background-color: #8f91a0;
    border-radius: 4px;
    color: #1e1f28; /* matches your dark background */
}

#NfTopBar QPushButton#btnClose {
    background-color: #c0c0c0;
}

#NfTopBar QPushButton:hover {
    filter: brightness(1.2); /* subtle brighten on hover */
}

#NfTopBar QPushButton:pressed {
    filter: brightness(0.9); /* subtle darken on press */
}

/* Side panels */

#NfPanel {
    width: 250px;
    min-width: 250px;
    max-width: 250px;

    /* subtle checker pattern */
    background-image: linear-gradient(
        45deg,
        #2e3140 25%,
        transparent 25%,
        transparent 75%,
        #2e3140 75%,
        #2e3140
    ),
    linear-gradient(
        45deg,
        #2e3140 25%,
        transparent 25%,
        transparent 75%,
        #2e3140 75%,
        #2e3140
    );

    background-size: 4px 4px;
    background-position: 0 0, 2px 2px;
}

/* Left panel */

#NfPanel[side="left"] {
    border-left: 1px solid #323546;
    border-bottom: 1px solid #323546;
    border-bottom-left-radius: 8px;
}

/* Right panel */

#NfPanel[side="right"] {
    border-right: 1px solid #323546;
    border-bottom: 1px solid #323546;
    border-bottom-right-radius: 8px;
}

/* Central area */

#NfCentralWidget {
    background-color: #20222e;
    border-bottom: 1px solid #323546;
}

/* Settings Dialog */

QDialog {
    background-color: #1e1f28;
    color: #e6eaf2;
    border-radius: 8px;
}

QTabWidget::pane {
    border: 1px solid #323546;
    background-color: #242632;
    border-radius: 6px;
}

QTabBar::tab {
    background: #2a2c3a;
    border: 1px solid #323546;
    padding: 6px 12px;
    border-top-left-radius: 4px;
    border-top-right-radius: 4px;
    margin-right: 2px;
}

QTabBar::tab:selected {
    background: #3ddcff;
    color: #0b0d12;
}

QPushButton {
    background-color: #242632;
    border: 1px solid #323546;
    border-radius: 4px;
    padding: 4px 12px;
}

QPushButton:hover {
    border: 1px solid #3ddcff;
}

QPushButton:pressed {
    background-color: #3ddcff;
    color: #0b0d12;
}

QLineEdit, QComboBox, QSlider {
    background-color: #20222e;
    color: #e6eaf2;
    border: 1px solid #323546;
    border-radius: 4px;
}

QCheckBox {
    padding: 2px;
}

/* ================ NfBrowserViewModeBar =============== */

/* Outer bar container */
NfDesktop--NfBrowserViewModeBar {
/*    background-color: #121824;
    border: 1px solid #232d3f;*/
    border-radius: 6px;
    padding: 2px;
}

/* Base button style */
NfDesktop--NfBrowserViewModeBar QToolButton {
    background-color: transparent;
    color: #94a3b8;
    border: 1px solid transparent; /* Reserve 1px border space to prevent layout jump */
    border-radius: 4px;
    font-size: 14px;
    padding: 0px 12px;
    margin: 0;
    height: 100%;
    width: 100%;
}

/* Hover state */
NfDesktop--NfBrowserViewModeBar QToolButton:hover {
    background-color: #1e293b;
    color: #f8fafc;
}

/* Active / Checked state */
NfDesktop--NfBrowserViewModeBar QToolButton:checked {
    background-color: #1a2332;
    color: #38bdf8;
}

/* Active + Hover state */
NfDesktop--NfBrowserViewModeBar QToolButton:checked:hover {
    background-color: #222d3f;
}

NfDesktop--NfBrowserViewModeBar QToolButton:focus {
    outline: none;
}

/* ================ NfBreadcrumbView ==================== */

/* Container widget */
NfDesktop--NfBreadcrumbView {
    min-height: 36px;
    max-height: 36px;
    border-radius: 6px;
    padding: 0px;
}

/* Individual breadcrumb items & navigation arrow buttons */
NfDesktop--NfBreadcrumbView QToolButton {
    background-color: transparent;
    color: #94a3b8;
    border-radius: 4px;
    font-size: 12px;
    padding: 3px 8px;
    margin: 0;
}

/* Hover state */
NfDesktop--NfBreadcrumbView QToolButton:hover {
    background-color: #1e293b;
    color: #f8fafc;
 }

/* Pressed state */
NfDesktop--NfBreadcrumbView QToolButton:pressed {
    background-color: #0b0d12;
    color: #38bdf8;
}

/* Active path item (Checked state) */
NfDesktop--NfBreadcrumbView QToolButton:checked {
    background-color: #1a2332;
    color: #38bdf8;
    font-weight: bold;
}

/* Disabled navigation arrows (when at start/end of model) */
NfDesktop--NfBreadcrumbView QToolButton:disabled {
    background-color: transparent;
    color: #475569;
}

NfDesktop--NfBreadcrumbView QToolButton:focus {
    outline: none;
}

/* Container resets */
NfDesktop--NfBrowserSortBar,
NfDesktop--NfBrowserFilterBar {
    background: transparent;
    border: none;
}

/* Flat interactive QToolButtons */
NfDesktop--NfBrowserSortBar QToolButton,
NfDesktop--NfBrowserFilterBar QToolButton {
    background: transparent;
    border: none;
    outline: none;
    color: #a0a5b5;
    padding: 2px 6px;
}

NfDesktop--NfBrowserTopBar NfDesktop--NfBrowserSortBar QToolButton:hover,
NfDesktop--NfBrowserTopBar NfDesktop--NfBrowserFilterBar QToolButton:hover {
    color: #ffffff;
    background-color: rgba(255, 255, 255, 0.05);
    border-radius: 4px;
}

NfDesktop--NfBrowserTopBar NfDesktop--NfBrowserSortBar QToolButton:pressed,
NfDesktop--NfBrowserTopBar NfDesktop--NfBrowserFilterBar QToolButton:pressed {
    color: #ffffff;
    background-color: rgba(255, 255, 255, 0.10);
    border-radius: 4px;
}

/* ================ NfDesktop::NfThumbnailsView ==================== */

NfDesktop--NfThumbnailsView {
    background-color: #20222e;
    border: none;
    outline: none;
    padding: 8px;
}

/* Base Thumbnail Card */
NfDesktop--NfThumbnailsView::item {
    background-color: #242632;
    color: #aeb5c0;
    padding: 8px;
    margin: 4px;
    border-radius: 4px;
    /* Subtle 1px light top/left highlight + dark bottom/right shadow */
    border-top: 1px solid #2e303f;
    border-left: 1px solid #2e303f;
    border-right: 2px solid #181922;
    border-bottom: 2px solid #181922;
}

NfDesktop--NfThumbnailsView::item:hover {
    background-color: #2a2c3a;
    color: #e6eaf2;
    border-top: 1px solid #35384a;
    border-left: 1px solid #35384a;
    border-right: 2px solid #13141c;
    border-bottom: 2px solid #13141c;
}

NfDesktop--NfThumbnailsView::item:selected {
    background-color: #1a2332;
    color: #ffffff;
    border-top: 1px solid #253247;
    border-left: 1px solid #253247;
    border-right: 2px solid #0f1520;
    border-bottom: 2px solid #0f1520;
}

NfDesktop--NfThumbnailsView::item:selected:hover {
    background-color: #222d3f;
    border-top: 1px solid #2e3d56;
    border-left: 1px solid #2e3d56;
    border-right: 2px solid #141b27;
    border-bottom: 2px solid #141b27;
}

NfDesktop--NfThumbnailsView::item:selected:!active {
    background-color: #282a36;
    color: #94a3b8;
    border-top: 1px solid #323544;
    border-left: 1px solid #323544;
    border-right: 2px solid #181921;
    border-bottom: 2px solid #181921;
}

