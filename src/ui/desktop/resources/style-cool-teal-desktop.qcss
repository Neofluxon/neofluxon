QWidget {
    font-family: "Noto Sans";
    background-color: #1e1f28;
    color: #aeb5c0;
    font-size: 13px;
}

QPushButton:hover {
    border: 1px solid #3ddcff;
}

QPushButton:pressed {
    background-color: #3ddcff;
    color: #0b0d12;
}

#container {
    border-radius: 8px;
}

/* Top bar and panels */

#NfTopBar, #NfPanel, #NfMainMenu {
    background-color: #242632;
    border: 1px solid #323546;
    border-radius: 2px;
}

#NfMainMenu {
    border: none;
}

/* Top bar */

#NfTopBar {
    border-top: 3px solid #323546;
    border-left: 1px solid #323546;
    border-right: 1px solid #323546;
    border-top-left-radius: 8px;
    border-top-right-radius: 8px;
    margin-bottom: 4px;
    padding: 2px;
    padding-left: 5px;
}

#NfTopBar QPushButton#btnMinimize,
#NfTopBar QPushButton#btnMaximize,
#NfTopBar QPushButton#btnClose {
    background-color: #8f91a0;
    border-radius: 4px;
    color: #1e1f28; /* matches your dark background */
}

#NfTopBar QPushButton#btnClose {
    background-color: #c0c0c0;
}

#NfTopBar QPushButton:hover {
    filter: brightness(1.2); /* subtle brighten on hover */
}

#NfTopBar QPushButton:pressed {
    filter: brightness(0.9); /* subtle darken on press */
}

/* Side panels */

#NfPanel {
    width: 250px;
    min-width: 250px;
    max-width: 250px;

    /* subtle checker pattern */
    background-image: linear-gradient(
        45deg,
        #2e3140 25%,
        transparent 25%,
        transparent 75%,
        #2e3140 75%,
        #2e3140
    ),
    linear-gradient(
        45deg,
        #2e3140 25%,
        transparent 25%,
        transparent 75%,
        #2e3140 75%,
        #2e3140
    );

    background-size: 4px 4px;
    background-position: 0 0, 2px 2px;
}

/* Left panel */

#NfPanel[side="left"] {
    border-left: 1px solid #323546;
    border-bottom: 1px solid #323546;
    border-bottom-left-radius: 8px;
}

/* Right panel */

#NfPanel[side="right"] {
    border-right: 1px solid #323546;
    border-bottom: 1px solid #323546;
    border-bottom-right-radius: 8px;
}

/* Central area */

#NfCentralWidget {
    background-color: #20222e;
    border-bottom: 1px solid #323546;
}

/* Settings Dialog */

QDialog {
    background-color: #1e1f28;
    color: #e6eaf2;
    border-radius: 8px;
}

QTabWidget::pane {
    border: 1px solid #323546;
    background-color: #242632;
    border-radius: 6px;
}

QTabBar::tab {
    background: #2a2c3a;
    border: 1px solid #323546;
    padding: 6px 12px;
    border-top-left-radius: 4px;
    border-top-right-radius: 4px;
    margin-right: 2px;
}

QTabBar::tab:selected {
    background: #3ddcff;
    color: #0b0d12;
}

QPushButton {
    background-color: #242632;
    border: 1px solid #323546;
    border-radius: 4px;
    padding: 4px 12px;
}

QPushButton:hover {
    border: 1px solid #3ddcff;
}

QPushButton:pressed {
    background-color: #3ddcff;
    color: #0b0d12;
}

QLineEdit, QComboBox, QSlider {
    background-color: #20222e;
    color: #e6eaf2;
    border: 1px solid #323546;
    border-radius: 4px;
}

QCheckBox {
    padding: 2px;
}

/* ================ NfBrowserViewModeBar =============== */

/* Outer bar container */
NfDesktop--NfBrowserViewModeBar {
    background-color: #121824;
    border: 1px solid #232d3f;
    border-radius: 6px;
    padding: 1px;
}

/* Base button style */
NfDesktop--NfBrowserViewModeBar QToolButton {
    background-color: transparent;
    color: #94a3b8;
    border: 1px solid transparent; /* Reserve 1px border space to prevent layout jump */
    border-radius: 4px;
    font-size: 11px;
    padding: 0px 12px;
    margin: 0;
    height: 100%;
    width: 100%;
}

/* Hover state */
NfDesktop--NfBrowserViewModeBar QToolButton:hover {
    background-color: #1e293b;
    color: #f8fafc;
}

/* Active / Checked state */
NfDesktop--NfBrowserViewModeBar QToolButton:checked {
    background-color: #1a2332;
    color: #38bdf8;
}

/* Active + Hover state */
NfDesktop--NfBrowserViewModeBar QToolButton:checked:hover {
    background-color: #222d3f;
}

NfDesktop--NfBrowserViewModeBar QToolButton:focus {
    outline: none;
}

/* ================ NfBreadcrumbView ==================== */

/* Container widget */
NfDesktop--NfBreadcrumbView {
    min-height: 36px;
    max-height: 36px;
    border-radius: 6px;
    padding: 0px;
}

/* Individual breadcrumb items & navigation arrow buttons */
NfDesktop--NfBreadcrumbView QToolButton {
    background-color: transparent;
    color: #94a3b8;
    border-radius: 4px;
    font-size: 12px;
    padding: 3px 8px;
    margin: 0;
}

/* Hover state */
NfDesktop--NfBreadcrumbView QToolButton:hover {
    background-color: #1e293b;
    color: #f8fafc;
 }

/* Pressed state */
NfDesktop--NfBreadcrumbView QToolButton:pressed {
    background-color: #0b0d12;
    color: #38bdf8;
}

/* Active path item (Checked state) */
NfDesktop--NfBreadcrumbView QToolButton:checked {
    background-color: #1a2332;
    color: #38bdf8;
    font-weight: bold;
}

/* Disabled navigation arrows (when at start/end of model) */
NfDesktop--NfBreadcrumbView QToolButton:disabled {
    background-color: transparent;
    color: #475569;
}

NfDesktop--NfBreadcrumbView QToolButton:focus {
    outline: none;
}
